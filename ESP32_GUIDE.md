# Panduan Integrasi ESP32 ke Next.js API & Supabase Database

Dokumen ini berisi penjelasan cara menghubungkan sensor ESP32 ke API endpoint Next.js yang telah terproteksi token dan terhubung ke tabel database Supabase `public.sensor`.

## Schema Tabel Database
```sql
create table public.sensor (
  id bigint generated by default as identity not null,
  moisture character varying not null,
  humidity character varying null,
  temperature character varying null,
  constraint sensor_pkey primary key (id)
) TABLESPACE pg_default;
```

## 1. Konfigurasi Endpoint & Token
- **Token API (di env):** `bdfe0835301faa88f2f983y3498yt90b5cf8af0f2d163c6f67877c684c03fe90c1` (sesuai file `.env` kamu).
- **Endpoint URL:** 
  - Lokal: `http://<IP_LAPTOP_KAMU>:3000/api/sensor`
  - Production: `https://<domain-kamu>.vercel.app/api/sensor`

---

## 2. Cara Test Menggunakan cURL
Kamu bisa menguji endpoint ini terlebih dahulu dari terminal laptop/PC kamu:

### A. Test GET (Mengecek Status API)
```bash
curl -X GET "http://localhost:3000/api/sensor" \
  -H "x-api-token: bdfe0835301faa88f2f983y3498yt90b5cf8af0f2d163c6f67877c684c03fe90c1"
```
**Response sukses (200 OK):**
```json
{
  "success": true,
  "message": "ESP32 Sensor API is online and authenticated.",
  "timestamp": "2026-07-03T07:15:30.000Z"
}
```

### B. Test POST (Mengirim & Menyimpan Data ke Database)
```bash
curl -X POST "http://localhost:3000/api/sensor" \
  -H "Content-Type: application/json" \
  -H "x-api-token: bdfe0835301faa88f2f983y3498yt90b5cf8af0f2d163c6f67877c684c03fe90c1" \
  -d '{
    "moisture": "720",
    "humidity": "65.5",
    "temperature": "27.8"
  }'
```
**Response sukses (200 OK) yang mengembalikan data hasil insert di Supabase:**
```json
{
  "success": true,
  "message": "Sensor data saved to database successfully",
  "data": {
    "id": 1,
    "moisture": "720",
    "humidity": "65.5",
    "temperature": "27.8"
  }
}
```

---

## 3. Contoh Kode Arduino/C++ untuk ESP32
Berikut adalah kode lengkap untuk ESP32 menggunakan library `WiFi.h`, `HTTPClient.h`, dan `ArduinoJson.h` (pastikan Anda menginstal library **ArduinoJson** di Arduino IDE).

```cpp
#include <WiFi.h>
#include <HTTPClient.h>
#include <ArduinoJson.h> // Membutuhkan library ArduinoJson (v6 atau v7)

// ----------------------------------------------------
// KONFIGURASI WIFI & API
// ----------------------------------------------------
const char* ssid = "SSID_WIFI_KAMU";           // Ganti dengan nama WiFi kamu
const char* password = "PASSWORD_WIFI_KAMU";   // Ganti dengan password WiFi kamu

// Gunakan alamat IP laptop/PC kamu jika menjalankan Next.js secara lokal.
// Jangan gunakan 'localhost' karena ESP32 adalah perangkat terpisah.
const char* serverUrl = "http://192.168.1.8:3000/api/sensor"; // Ganti IP sesuai IP laptop kamu (contoh: 192.168.1.8)
const char* apiToken = "bdfe0835301faa88f2f983y3498yt90b5cf8af0f2d163c6f67877c684c03fe90c1"; // Token dari file .env

// Pin sensor kelembapan tanah (Analog Pin)
const int soilPin = 34; 

void setup() {
  Serial.begin(115200);
  delay(1000);

  // Konek ke WiFi
  WiFi.begin(ssid, password);
  Serial.print("Connecting to WiFi");
  while (WiFi.status() != WL_CONNECTED) {
    delay(500);
    Serial.print(".");
  }
  Serial.println("");
  Serial.println("WiFi connected!");
  Serial.print("IP address: ");
  Serial.println(WiFi.localIP());
}

void loop() {
  if (WiFi.status() == WL_CONNECTED) {
    HTTPClient http;

    // Mulai koneksi HTTP ke URL server
    http.begin(serverUrl);

    // Set Header wajib
    http.addHeader("Content-Type", "application/json");
    // Token validasi dikirim lewat header "x-api-token"
    http.addHeader("x-api-token", apiToken);

    // Membaca nilai dari sensor kelembapan tanah
    int rawMoisture = analogRead(soilPin);
    
    // Konversi nilai analog ke persen kelembapan (sesuaikan dengan kalibrasi sensor kamu)
    // Contoh: sensor output 4095 saat kering sekali, 1500 saat sangat basah
    float humidityPercent = map(rawMoisture, 4095, 1500, 0, 100);
    humidityPercent = constrain(humidityPercent, 0, 100);

    // Dummy temperatur (bisa diganti dengan data sensor asli seperti DHT11/DHT22 jika ada)
    float temperature = 28.5;

    // Membuat JSON document sesuai skema tabel sensor di Supabase
    StaticJsonDocument<200> doc;
    doc["moisture"] = String(rawMoisture); // dikirim sebagai string sesuai skema database
    doc["humidity"] = String(humidityPercent);
    doc["temperature"] = String(temperature);

    // Serialisasikan JSON ke string
    String requestBody;
    serializeJson(doc, requestBody);

    Serial.println("Sending data: " + requestBody);

    // Melakukan HTTP POST request
    int httpResponseCode = http.POST(requestBody);

    if (httpResponseCode > 0) {
      String response = http.getString();
      Serial.print("HTTP Response Code: ");
      Serial.println(httpResponseCode);
      Serial.print("Response: ");
      Serial.println(response);
    } else {
      Serial.print("Error on sending POST request: ");
      Serial.println(httpResponseCode);
    }

    // Akhiri sesi HTTP
    http.end();
  } else {
    Serial.println("WiFi Disconnected. Reconnecting...");
    WiFi.begin(ssid, password);
  }

  // Kirim data setiap 10 detik (silakan sesuaikan kebutuhan)
  delay(10000);
}
```

## 4. Penting: Row Level Security (RLS) di Supabase
Jika data tidak masuk ke database dan mengembalikan error `401` / `403` dari Supabase, pastikan kebijakan RLS pada tabel `sensor` sudah diizinkan untuk insert publik:
Jalankan query ini di SQL Editor Supabase kamu jika ingin mengaktifkan akses insert publik untuk API Anon:
```sql
ALTER TABLE public.sensor DISABLE ROW LEVEL SECURITY;
```
*Atau buat policy spesifik jika RLS tetap aktif:*
```sql
CREATE POLICY "Allow anon insert" ON public.sensor 
FOR INSERT TO anon 
WITH CHECK (true);
```
